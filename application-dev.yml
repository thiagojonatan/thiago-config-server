spring:
  application:
    name: greenting-service
  cloud:
    config:
      enabled: false
greenting-service:
  greenting: "Hello"
  default-value: "World"
management:
  endpoints:
    web:
      exposure:
        include:
          - '*'
