spring:
  application:
    name: greenting-service
  cloud:
    config:
      enabled: false
greenting-service:
  greenting: "Pronto"
  default-value: "Mondo"
management:
  endpoints:
    web:
      exposure:
        include:
          - '*'
